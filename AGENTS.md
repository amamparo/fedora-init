# AGENTS.md

This file provides guidance to coding agents (Claude Code included) when working with code in this repository. Keep it named `AGENTS.md` with NO `CLAUDE.md` beside it: Claude Code's default `instructionFiles` mode (`claude-md-or-agents-md`, verified in the 2.1.283 binary) loads a project's AGENTS.md only when the project has no CLAUDE.md of its own, so even a stub CLAUDE.md would silently shadow this entire file.

## Every change to this machine lands in the repo

This repo is the machine's source of truth. Any change the user asks for to this machine — an app's behavior, a setting, a launcher, a keybinding, a package — is made in the role that owns that concern (or a new role) and committed and pushed; never only as a hand-edit of the live host (`~/.config`, dconf, `/etc`, a `.desktop` under `~/.local/share`), and never answered with manual steps when it can be automated. A "how do I…" question about this machine is such a request. Hand-edits are lost on the next fresh install and are often silently reverted by the next `./install.sh` (roles copy or template repo-owned files such as the Ghostty config, the zshrc and launchers). The live host picks the change up when the user runs `./install.sh <role>` — tell them so; don't run it yourself (see What this is). Only what genuinely cannot be automated (browser sign-ins, GitHub signing-key enrollment) stays manual, and even then the role surfaces it (a debug nudge) and the docs record it.

## Keep documentation in sync

When a change alters behavior, commands, role conventions, keybindings, or gotchas, update the documentation in the same change — this file, README.md, and any task-file header comments that describe the affected behavior. README.md is the user-facing doc (what the roles do and how to run them); AGENTS.md is the contributor-facing doc (architecture, conventions, gotchas). A PR that changes what a role does but not the docs is incomplete.

## Commit and push at good stopping points

Whenever the work reaches a coherent, self-contained stopping point — a role added, a gotcha fixed, docs synced — commit and push without waiting to be asked. A "good stopping point" means the tree is consistent: docs match behavior (see above), `ansible-playbook site.yml --syntax-check` passes, and the change stands on its own. This repo does NOT use PRs or feature branches: commit straight to `main` with a descriptive message and `git push origin main`. Do NOT run the playbook to "verify" before committing — the safe checks in the section below are the gate.

## What this is

An Ansible playbook that configures a fresh Fedora Workstation (GNOME) install, targeting Fedora 44 / GNOME 50. The only entry point is:

```sh
./install.sh                # bootstrap the toolchain + run every role
./install.sh battery zsh    # run roles whose tag contains a substring
./install.sh --check        # dry run (any dash-arg passes through to ansible-playbook)
curl -fsSL https://raw.githubusercontent.com/amamparo/fedora-init/main/install.sh | bash   # no checkout
```

**Running the playbook mutates the host** (installs/removes dnf packages, masks systemd units, changes the login shell). Don't execute it to "test" a change unless the user asks — even `--check` prompts for sudo. The safe checks are:

```sh
ansible-playbook site.yml --syntax-check
ansible-lint --offline        # policy in .ansible-lint; passes the production profile
printf '%s\n' install.sh scripts/*.sh | xargs -n1 bash -n   # bash -n parses only its FIRST argument, and a for-loop's status is its LAST iteration's; xargs exits 123 if any file fails
python3 -m py_compile library/*.py
```

`just check` runs exactly that gate (the justfile's other recipes: `install` wraps `./install.sh`, `seed-bitwarden` is the vault upsert — see the aws gotcha).

## Architecture

- `install.sh` — orchestrator and bootstrapper. If it can't find `site.yml` next to itself (piped from curl, or a stray lone copy), it downloads the GitHub tarball to a temp dir and re-runs from that copy. Otherwise it: reattaches stdin to `/dev/tty` (under curl|bash stdin is the exhausted pipe; the sudo prompt and the keyring pause both need the terminal), authenticates sudo **once, interactively** (`sudo -v` — password or fingerprint), installs a one-line sudoers drop-in (`/etc/sudoers.d/fedora-init`: `Defaults timestamp_type=global`, visudo-checked before install, guarded by content compare; the first install flips the record type, so that run authenticates a second time to write a global record), starts the `sudo -n -v` keepalive loop, installs the toolchain guarded by `rpm -q` (ansible-core, the community.general and ansible.posix collection rpms, python3-libdnf5, python3-psutil, rsync — all stock Fedora packages), maps bare arguments onto tags by substring against the role dir names under `roles/<group>/` (it walks `roles/*/*/`, skipping any dir without `tasks/main.yml`, so group names never match — see the `roles/<group>/<name>/` bullet), bootstraps the Bitwarden secrets session when due (see the aws gotcha; the bw install/sign-in helpers live in `scripts/bw-lib.sh`, sourced by install.sh and by `scripts/seed-bitwarden.sh`; due = the aws role's seed target (`~/.aws/credentials`) is missing and the tag selection reaches it, never under `--check`/`-C`; a failed sign-in warns and continues rather than aborting the run; the shared EXIT trap `bw lock`s a session this run opened even when the play aborts — a valid BW_SESSION inherited from the shell is borrowed and left alone (`bw_session_opened` in the lib; the function sets BW_SESSION in the caller's shell rather than printing it, because a `$(...)` caller would lose that flag in a subshell); **the sync is part of opening the session**: `bw unlock` only derives the key locally and succeeds on a persisted login whose refresh token has expired, so the first server call — `bw sync` — is what fails, with `invalid_grant` (seen live 2026-09-20), and the lib answers that with `bw logout` + a fresh `bw login`; any other sync failure sets `bw_synced` empty, which install.sh tolerates (a cached vault can still serve the item) and `scripts/seed-bitwarden.sh` treats as fatal (edits need the server) — and sweeps the bw download tempdir), and runs `ansible-playbook site.yml --diff` with **no become password at all**: every per-task sudo authenticates via the global timestamp record, skipping PAM entirely. This design is forced, not chosen: on fingerprint-enrolled Fedora, pam_fprintd runs before pam_unix in sudo's PAM stack and — probe-verified in ansible's exact become context (pty stdin, piped stdio) — blocks silently in the fingerprint wait without ever emitting a password prompt, so become-with-password times out no matter how the password is delivered; and sudo's default tty-scoped timestamps can never match become's fresh throwaway ptys, which is why the drop-in scopes the record to the user (trade-off: while a record is live, any same-user process can sudo without re-auth — acceptable on this single-user machine, documented in README, reverted by deleting the drop-in).
- `site.yml` — one play: `hosts: localhost`, local connection, `gather_facts: false`, **`become: false` at play level**, `force_handlers: true` (a handler notification lost to a mid-play abort is never re-fired by a converged re-run). A `package_facts` pre-task (tagged `always`, `check_mode: false`) is the rpm -q guard layer. Roles run in the old NN-module order; each is tagged with its role name, underscores as hyphens.
- `roles/<group>/<name>/` — one concern per role: `tasks/main.yml`, optional `handlers/main.yml`, assets in `files/`. Roles are grouped by context (2026-09-20): `system` (updates, snapshots, hostname, battery, fingerprint, login_keyring, multimedia, tailscale), `desktop` (window_snapping, no_overview, appindicator, gnome_prefs, ghostty, brave, steam, gimp), `dev` (zsh, cli_tools, git_workspace, github_cli, aws, vscode, podman, mise), `ai` (claude_code, claude_desktop, herdr), `audio` (pipewire, qpwgraph, reaper). The group is only a directory to `site.yml`, the tags and the matcher — but ansible finds roles ONLY through `ansible.cfg`'s `roles_path`, a closed list of the five group dirs (plus bare `roles/` for `common`; `roles_path` takes no globs), so a NEW group must be appended there or its roles are found by the matcher and then fail to load; `site.yml` names roles without the group, tags are still the bare role name, and install.sh's matcher walks `roles/*/*/` (skipping anything without `tasks/main.yml`), so `./install.sh <substring>` matches role names only, never group names (`ai` reaches tailscale, not the ai group). Inside a role `role_path` is the grouped dir, and anything that needs a SIBLING role's files must not hard-code `roles/<name>` — the gnome_extension include takes an absolute `ext_src` that callers `set_fact` from their own `role_path` before including, because `vars:` on `include_role` are templated lazily inside `common` (verified: `role_path` there is `roles/common`). Adding a role = pick the group, drop `roles/<group>/<name>/`, add it to `site.yml`. `roles/common/tasks/gnome_extension.yml` is a shared include (parameters: `ext_uuid`, `ext_src`, `ext_has_schemas`) used by window_snapping and no_overview; `roles/common/tasks/herdr_install.yml` (parameter: `herdr_manifest`) is the other, shared by its role (first install) and updates (upgrade). All go in via `include_role: name: common, tasks_from: …`.
- `justfile` + `scripts/` — `just install [args]` (`./install.sh "$@"`, positional-arguments so quoting survives), `just check` (the safe gate above) and `just seed-bitwarden` (`scripts/seed-bitwarden.sh`, upserts the vault items the roles read; see the aws gotcha). `scripts/bw-lib.sh` is the bw install/sign-in shared with install.sh.
- `library/` — two custom check-mode-aware modules for the D-Bus mutations nothing stock covers: `mutter_display_config.py` (DisplayConfig `ApplyMonitorsConfig`) and `login_keyring.py` (gnome-keyring `ChangeWithMasterPassword`). Both must run as the desktop user on the session bus, never under become.
- `ansible.cfg` / `hosts.ini` — local inventory, `gathering = explicit`, in-repo `roles_path`/`library`.

## Conventions (enforced by review, not tooling)

- **One concern per role — and an add-on belongs to its host's role.** Something that only exists to extend another thing this repo installs goes in that thing's role, not its own: the caveman plugin lives in claude_code, podman-tui/podman-docker live in podman. The test is dependency direction: if the candidate is meaningless without the host, merge it. Do NOT merge siblings that merely share machinery (window_snapping and no_overview both use the gnome_extension include but are separate concerns) or incidental coupling (mise's shell hook rides the zsh role's zshrc asset, but a runtime manager isn't shell config). When merging costs a tag, the substring matcher usually still covers it (`./install.sh claude` reaches the caveman tasks).
- **become is opt-in per task**, only for system mutations (dnf, /etc, systemd, usermod). Anything touching the user session — dconf writes, session D-Bus, `$HOME` files, user-level installers — must NOT have become: run as root it would configure root's desktop, the classic failure the old bash modules guarded with "no sudo here" comments. Never set become at play level.
- **Zero-network converged re-run** is a hard requirement — with exactly two deliberate exceptions: the updates role (drift from upstream is undetectable offline — and that is where the out-of-dnf herdr upgrade lives, presence-guarded) and podman's always-latest install (aaron's call; see both gotchas). The dnf5 module refreshes repo metadata even for no-op transactions, so every package task guards on `ansible_facts.packages` (exact rpm names — virtual provides can't false-match). Same discipline for anything else that would touch the network when nothing changed (the oh-my-zsh clone gates on its payload file; the tarball/curl installers gate on presence probes).
- **Check-mode honesty**: `./install.sh --check` on a converged machine must print every task ok/skipped with zero diffs — and on a *fresh* machine it must not crash. Read-only probes that later conditionals depend on get `changed_when: false` + `check_mode: false` (so they still run under --check). Mutating commands get an explicit `changed_when`. Registered results of tasks *skipped* in check mode lack attributes — guard downstream dereferences (`foo.path is defined`), the pattern the tempfile-using roles follow. Two fresh-`--check` gates recur: dnf5 resolves package specs even in check mode, so an install whose repo file was only *simulated* needs `not (ansible_check_mode and <repo_copy> is changed)` (brave, vscode) or `not ansible_check_mode or <release rpm> in ansible_facts.packages` (multimedia); and systemd_service fails on units whose package install was only simulated (battery).
- **Handler names resolve play-wide, not per-role — never share one across roles.** ansible-core's `search_handlers_by_notification` matches by NAME across the WHOLE play and, iterating in reverse, the LAST loaded handler with that name wins; a `notify:` in one role can land on an identically-named handler defined by a different role, which may sit at a different point in the handler list than the notifying role intended (reproduced live: a shared `systemctl daemon-reload` name sent a restart running against a stale unit). Give every handler a name unique to its role (`Restart ollama`, not a bare `daemon-reload`), or, when a role only needs a one-off command a shared name would collide with (two roles both wanting "Refresh the desktop database"/"Refresh the user icon cache" for their own icons), inline the command instead of adding a handler at all — gated on `register: x` + `when: x is changed` (see brave, claude_desktop, herdr).
- `gather_facts` is false: `ansible_env` does not exist. Use `{{ lookup('env', 'HOME') }}` (controller == target under the local connection).
- dconf values are **GVariant text**: strings `"'prefer-dark'"`, booleans `"true"`, doubles `"0.19"`, empty string arrays `"@as []"` (a bare `[]` is untyped and invalid), non-empty `"['a', 'b']"`. When a value comes from a YAML list var, render it explicitly (`| string`) — a bare single-variable template resolves to a native list, not text.
- Comment density is a feature: the task files carry the WHY comments the bash modules had. Keep lint clean (`.ansible-lint` skips exactly two style rules, with reasons in that file); prefer a `# noqa: rule` with justification over weakening a task.

## The GNOME extensions

`roles/desktop/window_snapping/files/rectangle@amamparo/` and `roles/desktop/no_overview/files/no-overview@amamparo/` are bundled, self-authored GNOME Shell extensions (ESM, GNOME 48–51), not vendored third-party ones — edit them directly here. The shared `gnome_extension.yml` mirrors them into `~/.local/share/gnome-shell/extensions/` with rsync; its rsync_opts are load-bearing for convergence (`--exclude=gschemas.compiled` — exists only in the installed copy; `--checksum` — tarball and git-checkout mtimes differ for identical content; `--omit-dir-times` — glib-compile-schemas rewrites schemas/ and bumps the dir mtime, which would re-report changed and re-compile forever). Schemas compile only when the sync changed (rectangle only). Enablement **merges additively** into `/org/gnome/shell/enabled-extensions`: the effective list is probed via `gsettings get` (NOT `dconf read` — vendor defaults like Fedora's background-logo live in gschema overrides, invisible in the user db) and the repo's `gnome_extensions_enabled` (site.yml) is unioned in — never overwritten, matching the old `gnome-extensions enable` append semantics; the "shell hasn't rescanned" race is gone because the write path IS the old fallback.

For rectangle: keybindings are declared in `schemas/*.gschema.xml` (the `as`-typed keys), consumed by name in `extension.js` (`KEYS`); `_unmaximize()` carries a GNOME ≤48 vs ≥49 API fallback. `metadata.json`'s `shell-version` gates which GNOME versions load an extension, and Wayland can't hot-reload GNOME Shell — extension changes take effect at logout/login.

## Gotchas encoded in the roles

- updates: sanctioned exception to the offline-converged-re-run rule, and it runs FIRST — later roles then resolve against fresh metadata. Its very first task writes `max_parallel_downloads=8` into `/etc/dnf/dnf.conf` (`community.general.ini_file`, become): the ONLY safe parallelism in this single-host play. libdnf5 downloads 3 packages at a time by default; every dnf transaction in the repo inherits the bump, so download phases overlap while the serial rpm transactions (one lock) never do — order untouched, check-mode honest, idempotent. Cross-TASK concurrency was rejected on evidence: async `poll: 0` is the only intra-host mechanism and it STARTS the real background job even under `--check` (verified: `started=True` in a dry run), which would execute installs during `./install.sh --check` — a hard violation of check-mode honesty. `forks` does nothing with one host; `pipelining` is ssh-only (local connection here). 8 is the Fedora-mirror sweet spot (past ~10, mirrors throttle). `update_cache: true` is what actually delivers the fresh-metadata promise: `state: latest` alone trusts any cache inside libdnf5's expiry window, and hours-stale metadata referencing mirror-pruned rpms 404'd podman-tui twice before this was understood. `state: latest` on `*` carries a `package-latest` noqa — latest is the concern here and in podman, nowhere else. fwupd exit codes (verified live): `get-updates` rc 0 = updates available, rc 2 = nothing to do — hence `failed_when: rc not in [0, 2]`; `update --assume-yes --no-reboot-check` applies without prompting, and staged firmware takes effect at reboot (the role says so). fwupd/flatpak tasks are guarded on the packages existing — Workstation ships both, minimal installs may not. **The out-of-dnf upgrade rides here too** (presence-guarded, so a machine that never ran the herdr role skips): herdr (probe `herdr --version` against the current `https://herdr.dev/latest.json` manifest — the same manifest herdr's own installer and `herdr update` read — and re-run the shared `herdr_install` include, which is a plain checksum-verified `get_url` to `~/.local/bin/herdr`, no become; see the herdr bullet). Under `--check` the version probe runs (read-only, forced with `check_mode: false`) and prints a debug naming the pending herdr version; the upgrade task itself still executes (`get_url`'s check-mode support is PARTIAL, verified via `ansible-doc` — unlike `uri`'s none — so it predicts a changed status without actually downloading, rather than being skipped the way the old ollama/goose includes were, `command` having no check-mode support there).
- snapshots: snapper + libdnf5-plugin-actions + btrfs-assistant (all stock rpms) — pre/post btrfs snapshots of / around every dnf5 transaction, ordered right after updates so the rest of the very first run is already covered. `files/snapper.actions` is upstream dnf5's canonical DNF4-snapper-emulation example with two deliberate deltas, both explained in its header: `-c number` on both `snapper create` calls (number-cleanup only prunes snapshots carrying the "number" cleanup algorithm — upstream's example accumulates forever) and an inline python3 rpmdb WAL checkpoint before the post snapshot (rpm's sqlite runs WAL mode; a snapshot with unflushed frames makes rpm report packages that `snapper undochange` already reverted — SysGuides' F44 finding; python3 stdlib, no sqlite3 CLI dependency). Actions format: FIVE colon-separated fields, backslash-escaped spaces stay one argv element, `${...}` substitutes before unescaping, and there is NO shell unless the command is `sh -c` — the python line's quotes are python syntax, not shell. Plugin errors never abort the transaction (raise_error defaults to 0). Config creation is `create-config` guarded by `creates:` on /etc/snapper/configs/root; retention limits are SEEDED one-time on creation (`set-config` rewrites the file — converged management would fight snapper, the reaper.ini class), followed by a one-time `restorecon -RF /.snapshots` (the fresh subvolume is unlabeled). Timers ship disabled per Fedora's spec; enablement carries the battery fresh-`--check` gate. Scope: /home is a separate subvolume, so root snapshots never cover user files (same-disk undo only — offsite backup is deliberately out of scope here); /var IS inside root, so rollbacks rewind logs and system-side container state. Post-rollback trap documented in the role, not automated: dnf failing with "cannot copy: packages.toml" wants `mkdir -p /usr/lib/sysimage/libdnf5` + restorecon. Substring: `./install.sh snap` matches BOTH window-snapping and snapshots.
- hostname: sets the static hostname to `thinkpad` (LAN-reachable as `thinkpad.local`) via `ansible.builtin.hostname` with an explicit `use: systemd` — there is NO `FedoraHostname` class (verified: 35 distro subclasses, zero mention of Fedora), so autodetection rides one `is_systemd_managed()` canary whose fallback is base `Hostname` → `UnimplementedStrategy`, a HARD FAIL rather than a degrade; naming the strategy skips that path. `become: true` because `SetStaticHostname` is the polkit action `org.freedesktop.hostname1.set-static-hostname` (auth_admin_keep), which root short-circuits — no agent, no prompt under this repo's passwordless become. **The handler MUST be `state: restarted`, never `reloaded`:** avahi consults `gethostname()` exactly ONCE, in `avahi_server_new()`, and watches nothing afterwards (inotify covers only /etc/avahi and /etc/avahi/services; no `org.freedesktop.hostname1` subscription — `strings /usr/bin/avahi-daemon | grep hostname1` is empty; netlink is RTMGRP_LINK/IFADDR, interfaces not names), while `reload_config()` — reached by SIGHUP, `avahi-daemon -r`, and therefore the unit's own `ExecReload=` — reloads static services/hosts/resolv.conf/browse domains and NOT the host name, so a reload leaves the machine answering as the old name until reboot. The daemon's "-2" conflict rename is not a counterexample (it derives from the in-memory name in response to a packet). Equally, never write `/etc/hostname` with `copy`: it bypasses hostnamed, the running kernel name stays stale, and since avahi publishes whatever `gethostname()` returns the restart would then re-read the OLD name — the module sets permanent-then-transient (only `SystemdStrategy` overrides the base class's reverse order, "to avoid NetworkManager complaints"). **NEVER verify a rename with `getent`/`resolvectl` on this machine** — resolved answers the machine's own `<hostname>.local` locally without consulting avahi (mDNS is `-mDNS` in resolved on Fedora; avahi owns 5353): resolved says so itself, `resolvectl query fedora.local` closing with `Data from: synthetic` and returning every local address including ones avahi is not publishing, while a foreign `.local` name fails outright. Use `avahi-resolve -n <name>.local`, another host, or `busctl call org.freedesktop.Avahi / org.freedesktop.Avahi.Server GetHostName` (which also catches a silent `-2`). **avahi/nss-mdns are ASSUMED, never installed** (the updates-role fwupd/flatpak guard-on-presence precedent): Workstation ships both, and `nss-mdns`'s posttrans scriptlet runs `authselect enable-feature with-mdns4` — installing it would drag this repo into authselect, whose `with-fingerprint` the whole become design rests on. The guard is a PLAIN `'avahi' in ansible_facts.packages`, deliberately NOT the tailscale/battery `not ansible_check_mode or …` form: this role installs nothing, so an absent avahi is genuinely absent rather than simulated, and the tailscale form would evaluate true on a real run and hard-fail systemd_service on a unit that will never exist. Do NOT touch `/etc/hosts` (`files` precedes `myhostname` in the nsswitch hosts line, so a `127.0.0.1 thinkpad` entry would SHADOW the real LAN address; "fedora" is in no hosts file today and resolves fine both ways — the `sudo: unable to resolve host` folklore predates nss-myhostname), do NOT swap `mdns4_minimal` for dual-stack `mdns_minimal` (nss-mdns's README: IPv6-first apps time out), do NOT add a firewalld task (default AND active zone is FedoraWorkstation, which already permits 1025-65535/udp, so 5353 is open — the `mdns` service entry would fork a vendor zone file into a frozen copy for nothing), do NOT enable `publish-workstation` (pristine `config(noreplace)`; `_workstation._tcp` is the administrative "browse for all PCs on the LAN" advert in `avahi-daemon.conf(5)`'s own words, consumed by avahi-browse/avahi-discover and NOT by file managers, orthogonal to the `.local` A record, and avahi names it `<hostname> [<mac address>]` — a MAC broadcast on every roamed network). Today's "fedora" is `DEFAULT_HOSTNAME` in `/usr/lib/os-release` (fedora-release-identity-workstation), NOT DHCP and NOT `/etc/hostname` — which exists holding one newline as `%ghost %config(noreplace)` from systemd; `hostnamectl --json=short` reports HostnameSource `default`, StaticHostname null. A valid static name is also NetworkManager's own documented opt-out from touching the hostname, so no `hostname-mode=none`. The module's `--pretty --static` call really does set the pretty name (hostnamectl drops a redundant one only on its no-flags path), CREATING `/etc/machine-info` and moving GNOME's "Device Name" and the Bluetooth adapter alias — SEEDED once, never converged, since change detection reads only `--static`. Ordered slot 3, after snapshots (inside the first run's rollback coverage) and BEFORE tailscale, so the first-ever `tailscale up` registers `thinkpad`; never `tailscale set --hostname`, which permanently un-sticks the tailnet name from the OS hostname. **Renaming strands Chromium profile locks, which is why the role has a second half.** Chromium — and every Electron app, which embeds it — locks a profile with a symlink whose NAME encodes machine+pid (`~/.config/BraveSoftware/Brave-Browser/SingletonLock -> fedora-13834`). A stale lock naming THIS machine self-heals (ProcessSingleton checks the pid, finds it dead, breaks the lock — which is why an unclean browser exit is normally invisible), but a lock naming a DIFFERENT machine is never broken, since a profile in a shared/NFS home may genuinely be held by another host where breaking it would corrupt it. So the rename converts a self-healing lock into a permanent one: `process_singleton_posix.cc:365` "The profile appears to be in use by another Brave process (13834) on another computer (fedora)". **The failure is SILENT** — the very next line is `message_box_dialog.cc:200 Unable to show message box: <same text>`, because the dialog explaining the refusal is drawn BY the browser that just refused to start, so nothing reaches the screen and clicking the launcher looks like a no-op with no error anywhere a user would look. That silence is why this is automated rather than documented. Diagnosed live 2026-08-16: Brave ran as `fedora` at 02:03, a 02:22 reboot killed it before cleanup, this role renamed the host at 02:26, and the browser never started again. A pristine machine CANNOT hit it — hostname is slot 3 and brave slot 14, so Brave isn't installed when the rename happens and its first-ever lock already reads `thinkpad`; the reachable case is the FIRST `./install.sh` on an already-used machine. Four tasks sweep it, all `$HOME` so NO become (root's profiles aren't the wedged ones): `find` `SingletonLock` symlinks under `~/.config` (depth 3 reaches `BraveSoftware/Brave-Browser/`; a missing path warns rather than fails, so a fresh machine walks zero entries — and `~/.var/app` is deliberately out of scope: the one flatpak this repo installs, Podman Desktop, is an Electron app that lands after this rename in role order, and flatpak's PID namespace makes the host-side liveness check meaningless for a sandboxed lock — its recorded pid is the sandbox's — so the manual `rm ~/.var/app/<id>/config/*/Singleton*` is the fix there), `stat` with **`follow: false`** to read the target (find does NOT return link targets, and following would report the always-dangling relative target — nothing is ever named `fedora-13834` — as missing and lose the name), select those whose recorded host ≠ `hostname_set.name`, then remove all three `Singleton*` files (Cookie and Socket carry no hostname but belong to the same lock generation; removing the set is what was verified to un-wedge Brave) **only when `/proc/<pid>` is gone**. Compare against `hostname_set.name`, never the live `hostname`: the module returns its own `name` param unconditionally (verified in its source — `kw = dict(changed=changed, name=name, ...)` has no check-mode branch), so it reads `thinkpad` even on the `--check` run where the rename was only simulated, and the name stays one literal. Split on the LAST hyphen (hostnames may contain them: `old-host-name-4242`); RFC 1123 limits them to letters/digits/hyphen, so interpolating the name into the regex is safe. **The pid-alive case is deliberately SKIPPED, not fixed** — a browser running across the rename still owns its lock and cleans it up on exit, and the asymmetry is the point: guessing "stale" wrongly lets a second instance into a live profile and corrupts it, while guessing "held" wrongly costs one manual `rm` (the residual pid-reuse case gets a debug saying exactly that). Substring: `./install.sh host` matches BOTH hostname and ghostty; `hostname`/`name` hit only this role.
- steam: from RPM Fusion nonfree, so it MUST stay ordered after multimedia (which installs the release rpms); carries the same fresh-`--check` gate on `rpmfusion-nonfree-release`.
- battery: `tlp` declares `Conflicts: tuned` (`tuned-ppd` merely depends on tuned). The stock stack is removed *before* installing so its orphaned deps get swept — but the dnf5 *module*, unlike the CLI, only sweeps with `autoremove: true` (it wires `clean_requirements_on_remove` to that flag, default off); the removals here and in brave set it. Removals are guarded per exact rpm name because `tlp-pd` *provides* `power-profiles-daemon`. Unlike the dnf5 CLI, the module no-ops when removing an absent package — the guards still matter for the offline re-run. `ansible.builtin.systemd_service` fails on units whose file doesn't exist (LoadState probe, even under --check), so power-profiles-daemon.service — whose unit vanished with the package — is masked via plain `systemctl mask` guarded by `creates:` on the mask symlink; the systemd-rfkill units ship with systemd itself and use the module, but enable/start of tlp.service+tlp-pd.service is gated `not ansible_check_mode or <rpm> in ansible_facts.packages` — on a fresh --check the install was only simulated and the module would hard-fail on the missing units. TLP's docs require masking systemd-rfkill. `tlp start` is a handler (fires only on change, where the bash module ran it every time; `force_handlers` keeps a notification from being lost to a later role's failure). powertop is measurement-only — no autotune service, it would fight TLP over the same sysfs knobs.
- window_snapping / no_overview: extension install + enablement is the shared `gnome_extension.yml` (see above). GNOME's stock bindings collide with all four tiling keys (`shift-overview-up/down` defaults to Super+Alt+Up/Down; `switch-to-workspace-left/right` includes Super+Alt+Left/Right) and Mutter resolves duplicates nondeterministically — the role clears the overview pair and rebinds workspace switching to Super+Alt+PageUp/PageDown. Check both `org.gnome.shell.keybindings` and `org.gnome.desktop.wm.keybindings` schemas before adding any new binding. Super+Alt+F is GNOME's native `toggle-maximized`, bound *alongside* its stock Alt+F10 (a bare set would silently drop it); `toggle-fullscreen` is reset (dconf `state: absent`) because an earlier revision bound it to the same key. For no_overview: opening the overview at login is hardcoded in gnome-shell (upstream gnome-shell!4009 unmerged as of GNOME 50); since GNOME 50 extensions initialize after the startup animation begins, so a brief flash is unavoidable. Don't add a second hide-overview mechanism alongside it — running two is a reported crash source.
- cli_tools: one guarded dnf5 transaction for the modern-CLI baseline (fzf ripgrep fd-find bat jq yq btop eza zoxide uv just vim-enhanced — all stock Fedora rpms; the battery difference-filter guard). Name traps, verified against the F44 package index: the rpm is `fd-find` but the binary is plain `fd` (Debian's fdfind rename doesn't apply), and Fedora's `yq` IS mikefarah's Go yq v4 (no go-yq rpm exists) — don't "correct" either. **vim MUST be listed as `vim-enhanced`** — this is where the exact-rpm-name convention grows teeth rather than being a style rule: there is no package called plain `vim` (`dnf5 repoquery --whatprovides vim` -> vim-enhanced, a virtual Provides), and package_facts keys only on REAL names, so a bare `vim` would resolve fine for dnf5 yet never match the difference-filter guard — re-resolving the whole eleven-package transaction over the network on every converged re-run. It is genuinely absent on a stock Workstation: the base group installs vim-*minimal* (/usr/bin/vi, a "Tiny version" with -syntax -eval -persistent_undo), verified by install reason (vim-minimal Group, vim-enhanced User). vim and `just` are the two entries needing NO zshrc wiring — vim's rpm ships /etc/profile.d/vim.sh (aliases vi=vim for interactive shells), just's ships a zsh site-functions completion — so don't add lines for them. Base editor only: `"+y` to the system clipboard lives in the separate vim-X11 (GVim) rpm, deliberately not installed. One role deliberately: a baseline toolkit is one concern; eight one-package roles would be noise. Shell wiring lives in roles/dev/zsh/files/zshrc, NOT here (the mise rule — a lineinfile would diverge from the zsh role's cmp-copied asset): fzf's bindings are installed-but-disabled by the rpm (`source /usr/share/fzf/shell/key-bindings.zsh`; its completion auto-loads from zsh site-functions — never source that half manually), zoxide ships no profile.d hook (the `eval "$(zoxide init zsh)"` is mandatory). uv complements mise, never replaces it: mise pins runtimes, uv does packaging/`uvx` (how many MCP-server configs launch stdio servers). Substring: `./install.sh cli` also sweeps github-cli; `tools` is the unique handle.
- zsh: oh-my-zsh via the git module, but gated on the payload file `oh-my-zsh.sh` (a dir without it is a broken half-install — moved aside to `~/.oh-my-zsh.broken` and recloned), NOT left to the git module's own idempotency: for an existing clone the module fetches origin every run, which would break the offline re-run and silently become an updater. The one-time `~/.zshrc.pre-fedora-init` backup (README references the name) is a cmp probe + guarded copy, not `backup: true` (which timestamps) — and guarded on the backup NOT already existing, because the cmp gate re-fires every time the zshrc asset itself changes (it did for the fzf and zoxide lines, for example) and would otherwise overwrite the original with fedora-init's previous zshrc.
- git_workspace: the ssh key is presence-guarded via `creates:` — NEVER regenerate an existing key (it may be enrolled with GitHub; silent replacement breaks every remote). No passphrase, deliberately (the login-keyring LUKS trade-off). The pubkey debug is gated `not ansible_check_mode` — under fresh --check the file doesn't exist to read. Nautilus bookmarks live in `~/.config/gtk-3.0/bookmarks` even for GTK4 Nautilus ("URI [label]" lines, picked up live); the bookmark is grep-probed by URI and only *added* when absent — a lineinfile regexp rewrite would reset a user-renamed label. That is the "bookmark rule" other bullets and roles cite: add or seed only when absent, and never rewrite what the user has since changed (contrast the dconf class — repo-declared values, converged). **SSH commit signing** rides here too: git's SSH backend signs commits+tags with the SAME ed25519 key (no separate GPG key), set via `community.general.git_config` `scope: global` — plus the global `user.name`/`user.email` a fresh gitconfig lacks (signing needs an author identity). All `~/.gitconfig`/`$HOME` writes, NO become (root's gitconfig otherwise — the dconf-as-root class). The `allowed_signers` file (local `git verify-commit`) is guarded on a `stat` of the pubkey (`check_mode: false`, so truthful under --check) — `lookup('file', …pub)` in its `content` would crash a fresh --check where the key was only simulated, the same trap the pubkey debug dodges. GitHub "Verified" needs the key re-added as a *Signing* key (separate slot, same key) — not automatable, so it's not attempted. **delta** rides here too (add-on rule — a git pager is meaningless without git): the `git-delta` rpm plus core.pager/interactive.diffFilter/delta.navigate/merge.conflictStyle=zdiff3 through the same git_config pattern — repo-declared converged values (the aws region/output class: edit the task, not `git config`), user scope, NO become. The pager engages only on a tty, so scripts and Claude Code see plain git output; delta.side-by-side stays unset deliberately (too wide for the laptop panel).
- github_cli: `gh` from stock Fedora repos (`updates`) — plain dnf5 + package-facts guard, NO vendor `.repo` and so NO fresh-`--check` repo gate (contrast vscode/tailscale). Separate role, not folded into git_workspace: gh authenticates via its own token/HTTPS flow independent of the ssh key, so the add-on merge test fails (like window_snapping vs no_overview). `gh auth login` is a browser/device flow, not automatable — surfaced as a debug nudge (gated `is changed and not ansible_check_mode`), the tailscale-`up` pattern. Substring quirk: `./install.sh gh` reaches ONLY ghostty — the tag `github-cli` does not contain "gh" (g-i-t-h-u-b), a doc error that stood until 2026-09-20; `./install.sh github` hits only this role.
- aws: awscli2 is a stock Fedora rpm (plain dnf5 + package-facts guard, the github_cli pattern). The access keys come from **Bitwarden** at seed time, never from the repo — and the sign-in lives in install.sh, NOT a task: the `community.general.bitwarden` lookup shells out to `bw` on the CONTROLLER, and bw's interactive prompts (email + master password + TOTP; the cloud can additionally demand the personal API-key client_secret, its bot check) need the tty tasks don't have. install.sh installs `bw` presence-guarded into `~/.local/bin` (single Node-bundled static binary — no rpm exists, no self-update, the reaper presence-is-terminal class; unzipped with `python3 -m zipfile`, dodging an unzip rpm) and exports `BW_SESSION`, gated three ways against needless prompts: seed target (`~/.aws/credentials`) absent + not `--check`/`-C` + tag selection reaches aws (the passthru `--tags` form is approximated by a word-scan, so `--skip-tags aws` is an accepted false positive — a needless prompt, nothing worse, since sign-in failure is non-fatal: install.sh warns and runs the play sessionless). `BW_SESSION` is inherited by every child the play spawns while it lives — the same single-user trade-off class as the sudo global timestamp, bounded by the EXIT trap's `bw lock` (which fires even on an aborted play — a success-only lock would strand a valid session exactly when a failed run leaves one). The role's fetch (set_fact) and seed (copy) BOTH carry `no_log` — install.sh always passes `--diff` and the rendered credentials file IS the secret — and gate on the same triple (probe absent, `BW_SESSION` non-empty, `not ansible_check_mode`; a session-less unconverged run prints a nudge instead of skipping silently). The lookup is exact-name (the plugin equality-filters bw's fuzzy `--search`) with `result_count=1` — a missing or ambiguous `aws` item fails loudly, never seeds an arbitrary match. Item shape: login item named `aws`, username = Access Key ID, password = Secret Access Key. **`just seed-bitwarden` is the reverse direction** (`scripts/seed-bitwarden.sh`): upserts that item from `SEED_AWS_ACCESS_KEY_ID` / `SEED_AWS_SECRET_ACCESS_KEY` or hidden tty prompts (blank = skip); only supplied values are written (an untouched item is neither created nor edited, an existing item keeps every field not supplied, nothing else in the vault is read or written), items are matched by EXACT name among items of ANY type — the set the roles' lookup sees, since `community.general.bitwarden` filters by name alone — with two same-named items or a same-named non-login aborting (creating a login next to a secure note of that name would be the very ambiguity that fails the play's result_count=1); creating `aws` needs both fields (a half item would seed a broken credentials file the aws role never revisits), updates may carry one; an exported-but-empty `SEED_*` skips without prompting; a BW_SESSION inherited from the shell is used and left unlocked (only a session the script opened is locked — `bw_session_opened` in `scripts/bw-lib.sh`, the same rule install.sh's trap now follows), new items start from `bw get template item`/`item.login`, secrets reach jq via its environment (never argv), and the EXIT trap `bw lock`s. `~/.aws/credentials` is SEEDED ONLY WHEN ABSENT — after that rotation via `aws configure` must win (the bookmark rule), and the absence gate doubles as the zero-network + zero-prompt convergence guard; region/output in `~/.aws/config` are repo-declared preferences managed converged via ini_file (the dconf class). All `$HOME` writes NO become. CLI 2FA limits: TOTP/email/YubiKey-OTP only — an account locked to passkey/FIDO2-only can't sign in from the CLI, so keep TOTP enrolled.
- appindicator: GNOME dropped legacy tray icons in 3.26; apps that still publish one (claude_desktop's TrayIconLinux.png, the tailscale applet) show nothing without this. Fedora ships `gnome-shell-extension-appindicator` (Workstation dep, UUID `appindicatorsupport@rgcjonas.gmail.com`, in `/usr/share/gnome-shell/extensions`) but DISABLED — so the role guards the package then enables the UUID. Deliberately does NOT use the shared `gnome_extension.yml` include: that rsyncs a bundled source tree + compiles schemas, and there's nothing to mirror for a system-installed extension — only the enablement half applies, reproduced here. Enablement is the same ADDITIVE `gsettings`-probe + `dconf` union as gnome_extension.yml (never a bare set — would drop Fedora's background-logo and the repo's rectangle/no-overview), user dconf so NO become, takes effect at next login (Wayland can't hot-reload the shell).
- multimedia: the old `dnf swap` non-idempotency becomes install-guarded-on-target + a guarded removal of the stock package. `allowerasing` alone equals `dnf swap` only for real Conflicts pairs (ffmpeg vs ffmpeg-free); the Intel driver pair *coexists* (Provides only, disjoint paths), so `libva-intel-media-driver` gets an explicit `state: absent` after the install — after, so there is never a driverless window. RPM Fusion release rpms install by URL, guarded per repo (a joint guard would re-download both when one is missing) and **need `disable_gpg_check: true`**: the dnf5 CLI leaves `localpkg_gpgcheck` off by default (why the bash flow worked) but the module forces it on, and a fresh rpmdb has no RPM Fusion key — the release rpms then install the repo keys, so subsequent rpmfusion packages ARE gpg-checked. The Fedora version comes from `lookup('pipe', 'rpm -E %fedora')` because facts are off (controller == target; change to a distribution fact if this ever targets remote hosts). intel-media-driver is hardware-specific: Intel Panther Lake ThinkPad T14 Gen 7 (Core Ultra 7 355, GPU 8086:b090 on the `xe` driver), not AMD — verified against `lspci -nnk` and `/sys/class/dmi/id/product_version`, which is where to re-check it rather than trusting this line.
- brave: default browser via `community.general.xdg_mime` (needs community.general ≥ 10.7.0; Fedora 44 ships 12.x) — as the user, never become (it writes `~/.config/mimeapps.list`). The default is set *before* the removals so http/https handlers never dangle; removals stay guarded (`item in ansible_facts.packages`) for the offline re-run. Firefox removal has a prerequisite: since the Anaconda WebUI installer (F42+ Workstation) fresh installs leave `anaconda-webui` on disk, and it declares the boolean dep `(firefox if fedora-release-workstation)` (it renders in Firefox) — dnf5 depsolve-fails the firefox removal rather than cascading through a rich dep, so a guarded task removes anaconda-webui (dead weight post-install, `autoremove` sweeps its dep chain) immediately before the browser loop. **PWA management was removed deliberately (2026-08-07) — don't reintroduce it.** The role used to force-install web apps via the Chromium `WebAppInstallForceList` policy in `/etc/brave/policies/managed/`; it left the machine in a wedged state that took manual cleanup: Brave's profile web-app registry (`Default/Preferences` -> `web_apps.web_app_ids`) came out EMPTY while the force-install had already written `~/.local/share/applications/brave-<appid>-Default.desktop` launchers + hicolor icons, so the apps were unremovable from Brave's UI (force-listed apps never offer uninstall) *and* unremovable by Brave at all (it no longer believed they existed) — orphaned launchers polluting GNOME app search forever. Web apps are now hand-installed (Brave ⋮ ▸ Cast, save, and share ▸ Install page as app), which registers them properly and leaves a working uninstall path. If those orphans ever recur, cleanup is: drop the policy file, quit Brave, `rm` the `brave-*-Default.desktop` entries + `~/.local/share/icons/hicolor/*/apps/brave-*-Default.png` + `Default/Web Applications/Manifest Resources/<appid>`, then `update-desktop-database ~/.local/share/applications`. **Presented as "Web Browser", still searchable as "Brave"** (2026-08-11): the shell shows `Name=Web Browser` while `Keywords=Brave;Browser;Internet;WWW;` keeps app search finding it (gnome-shell matches name, generic name, keywords, comment and the executable). This DOES shadow the vendor entry in `~/.local/share/applications`, which the claude_desktop rule appears to forbid — read that rule precisely: what it forbids is parking a **copied snapshot** of vendor-owned values in the dir that always wins. There is no per-key override for desktop entries (entries replace rather than merge across XDG dirs), so renaming an app is *only* possible with a shadow; what makes this one safe is that it is **GENERATED from the vendor file on every run** (slurp `/usr/share/applications/brave-browser.desktop` -> two `regex_replace` passes -> `copy: content:`) rather than vendored in `files/`. `Exec`/`MimeType`/`StartupWMClass`/`Icon` and both Desktop Actions come out byte-identical to whatever Brave currently ships — verified with `diff` — so nothing is frozen and the role's own `xdg_mime` task still has its MimeType list. Staleness is bounded to ZERO runs by role order: `updates` runs `dnf upgrade` FIRST in the same play, so a Brave upgrade is always followed by this regeneration before the play ends (upgrading Brave with dnf by hand is the one window where the shadow can lag; the next `./install.sh` closes it). Transform details that matter: pass 1 strips any `Keywords=` the vendor may add later (duplicate keys — GKeyFile takes the last, `desktop-file-validate` warns), pass 2 rewrites the FIRST `Name=` only via `count=1` (ansible-core ≥ 2.17; `[Desktop Entry]` is first per spec, and the later `Name=` lines belong to the new-window/new-private-window actions, which must keep their labels). The replacement carries a real newline from a YAML block scalar, NOT a `\n` escape inside the Jinja literal — the reaper reaper-mouse.ini lesson. `update-desktop-database` is a handler (never converged, so it must fire only on change). **Custom app icon** (a Netscape-styled Brave lion) rides here too and is unaffected by the rename: it overrides the icon THEME, and `Icon=brave-browser` in the generated entry resolves through it, which is the right layer — it also covers window icons and notifications, where the `.desktop` `Icon=` line never gets consulted. Overriding `/usr/share/icons/hicolor` is worse than useless: those PNGs are NOT rpm-owned (`rpm -ql brave-browser` doesn't list them — it ships only `/opt/brave.com/brave/product_logo_<size>.png`), because Brave's **postinstall scriptlet generates them** with `xdg-icon-resource install` on every install/upgrade, so the updates role's `dnf upgrade` would silently restore the vendor mark. `~/.local/share/icons/hicolor` is untouched by that scriptlet and outranks `/usr/share` per the XDG icon spec, so the override survives upgrades. Ships **exactly the seven sizes the scriptlet registers** (16 24 32 48 64 128 256): the spec's lookup loops the size subdir OUTSIDE the basedir list, so any size not shipped resolves to the vendor icon at that size and the app reads with a mixed icon depending on where GNOME draws it. PNGs are pre-scaled at authoring time (ImageMagick Lanczos from the 465×465 `files/icons/brave-browser-master.png`, which sits OUTSIDE `files/icons/hicolor/` so the recursive copy never installs it) — the reaper pre-merged-icon-pack precedent: no ImageMagick on the target, and better 16px quality than GTK downsampling a 465px source live. The copy's `src` trailing slash merges the tree's contents into the user theme; `$HOME` write so NO become (root's icon theme otherwise). `gtk-update-icon-cache -f -t` is a HANDLER (`-f` always rewrites, so it could never be converged — the reaper fc-cache pattern); `-t` is required because the user's hicolor dir has no `index.theme` and shouldn't have one (the theme is defined by `/usr/share/icons/hicolor/index.theme` and merged across basedirs), and the refresh is load-bearing rather than cosmetic because an `icon-theme.cache` already sitting in that directory (this handler's own previous run leaves one; so do some bespoke user-level app installers — `xdg-icon-resource` itself only refreshes a cache that already exists) hides the new files from GTK and from GNOME Shell, whose St icon code forks the same cache logic: the cache is trusted whenever its mtime is not older than `hicolor/` itself, and a file dropped into an existing size subdir never bumps that, so a fresh login would trust the stale cache just the same (verified 2026-09-18: fresh GTK3 and GTK4 processes both missed a newly copied PNG until `gtk-update-icon-cache -f -t` rewrote the cache, which also bumps the `hicolor/` mtime GTK polls, so running apps pick it up within seconds).
- gnome_prefs: everything as the user, no become (root's dconf otherwise). Wallpaper is `files/amber-d.jxl`, staged into `~/.local/share/backgrounds/` (where Settings itself drops user-picked wallpapers) and pointed at by `picture-uri`+`picture-uri-dark`+`picture-options` — both light AND dark keys set (color-scheme is prefer-dark, but setting both means a mode flip never falls back to Fedora's stock background); the URI is GVariant text `'file://<HOME>/…'` and HOME's leading `/` yields the canonical three-slash `file:///`. `picture-options: 'zoom'` = scale-to-fill. JPEG XL renders as a wallpaper only with the gdk-pixbuf JXL loader present (Workstation ships it; a minimal install would need libjxl's pixbuf module — deliberately not force-installed). Resolution + refresh-rate mode + scale are `library/mutter_display_config.py` (1680x1050, variable, 1.0 — the panel is 1920x1200): monitors.xml is owned by mutter's DisplayConfig D-Bus API (method 2 = persist + apply live; method 0 = verify-only, which is how to probe a candidate config without touching the display), and it applies only when the built-in panel (`is-builtin` monitor prop — a logical-monitor count would misfire docked lid-closed) is the sole active display, because positions are scale-dependent logical pixels and mutter *rejects* overlapping re-scaled layouts ("Logical monitors overlap") rather than repositioning, which would fail the play. Docked layouts are separate monitors.xml configs anyway. **VRR is a mode, not a flag** — there is no `enable_vrr` property: mutter lists every mode TWICE, the adaptive-sync twin carrying `refresh-rate-mode: 'variable'` and an id suffixed `+vrr` (`1680x1050@59.954+vrr`), so *selecting that mode is* enabling it, and monitors.xml records `<ratemode>variable</ratemode>`. The per-monitor props dict in `ApplyMonitorsConfig` implements exactly three keys (`underscanning`, `color-mode`, `rgb-range`) and **silently ignores everything else** — verified live, an `enable_vrr: true` or a `refresh-rate-mode` stuffed in there returns success and does nothing, so that mistake is undetectable from the result. Same trap for a known key at the wrong type. Not behind `org.gnome.mutter experimental-features` on GNOME 50 (verified: the key is `@as []` while VRR is live). The module therefore matches on `width`/`height`/`refresh_rate_mode` and never on a mode id — ids embed the panel's EDID refresh to three decimals, which no playbook should pin — taking the highest-refresh match (a resolution can appear at several rates: this panel lists 1920x1200 at both 60.001 and 59.950) with an id tie-break so the pick never depends on ordering. **Supported scales are per MODE, not per monitor** (1920x1200 offers 1.3333 here, 1680x1050 offers 1.75 instead), so the module validates the scale against the chosen mode and applies mutter's own double rather than the YAML float — verified live, mutter otherwise fails the call with `Scale 1.33333 not valid for resolution 1680x1050`. Failure split: hardware variation is reported and never fatal (a resolution the panel doesn't offer skips; a missing variable-rate twin falls back to the fixed mode, both surfaced through `applied: false` and the reporting debug task), but an unsupported *scale* fails loudly — the resolution having matched means we're on the intended hardware, so it's a playbook bug.
- ghostty: from the scottames COPR (the repo ghostty's official install docs name for Fedora — a community build, gpg-signed with its COPR key; `files/ghostty.repo` is the COPR-served file verbatim, `skip_if_unavailable` included). App-search-for-"terminal" works because the packaged desktop entry ships `Keywords=terminal;tty;pty;` — verified by extracting the rpm; no desktop-file override exists, so don't add one. Ptyxis is removed *after* ghostty installs (never leave the machine terminal-less — the brave ordering rule for terminals); removal is what delivers "not accessible, not in search". The Moonfly theme moved here from the retired gnome_prefs Ptyxis tasks — bundled with ghostty (verified in the rpm), set in the repo-owned `files/config`.
- fingerprint: two lines of systemd drop-in (`/etc/systemd/system/fprintd.service.d/stop-before-sleep.conf`) that stop fprintd before the machine sleeps. **fprintd is transient** — D-Bus activated by the greeter's pam_fprintd, exits ~30s after the last operation — and if a suspend lands inside that window the daemon rides through the sleep still holding an open USB handle. On resume that handle is a zombie: transfers return `endpoint stalled or request not supported`, the stale claim is never released (`Device was already claimed`), and pam_fprintd fails **with no grantor at all** (`op=PAM:authentication grantors=? res=failed`), so GDM/the lock screen silently drop the fingerprint option and offer only a password. Nothing surfaces an error anywhere a user would look, and the wedge clears itself when the instance idles out — which is exactly why it reads as random. Diagnosed live 2026-08-16 by interleaving `Started fprintd` / `Deactivated` against `PM: suspend entry`: every working cycle that day had fprintd stop BEFORE the suspend (02:23:42 stop → 02:24:36 suspend; 11:40:50 stop → 12:21:48 suspend), and both broken ones suspended 39s and 28s after a fingerprint prompt with no intervening stop. That interleave is the diagnostic — reproduce it before believing any other theory. **Why the handle zombies instead of reconnecting:** systemd's hwdb marks this sensor unsafe to keep across sleep (`systemd-hwdb query "usb:v06CBp00F9*"` → `ID_AUTOSUSPEND=1`, `ID_PERSIST=0`) and `60-autosuspend.rules` honours it at device add (confirm with `udevadm test /sys/bus/usb/devices/3-7`, which prints BOTH writes — a `head`-truncated grep will hide the persist one and fake a contradiction), but libfprint flips `power/persist` back to 1 at runtime: it is the only component that writes those paths (`strings /usr/lib64/libfprint-2.so.2` carries `/sys/bus/usb/devices/%d-%s/power/persist` and `Failed to disable USB persist by writing to %s`, and fprintd's unit grants `ReadWritePaths=/sys/devices` with the comment "Allow tuning USB parameters (wakeup and persist)"). With persist=1 a device that drops across suspend is silently reattached to the same fd rather than raising a clean disconnect. Do NOT chase that with a udev rule forcing `power/persist=0` — udev runs at add, libfprint's write comes later and wins, and stacking a second workaround is the no-overview trap. **NOT a TLP or power-saving bug, and that is the first wrong turn to avoid:** the sensor's `power/control=auto` comes from stock Fedora's `60-autosuspend.rules` acting on hwdb's `ID_AUTOSUSPEND=1`, so TLP setting it too is irrelevant and `USB_AUTOSUSPEND=0` would not change the value; runtime autosuspend also demonstrably works (`fprintd-verify` wakes the device `suspended` → `active` and reaches `Verify started!`). Hence its own role, NOT a battery add-on — the merge test fails on dependency direction. `Conflicts=sleep.target` is what performs the stop (starting a target stops units that conflict with it); `Before=sleep.target` is what makes the stop *finish* first, since systemd.unit(5) is explicit that Conflicts= carries no ordering of its own. Anchor on `sleep.target`, never `suspend.target`: systemd-suspend/-hibernate/-hybrid-sleep/-suspend-then-hibernate.service each declare `Requires=sleep.target` + `After=sleep.target`, so it is the one node all four modes share and it is reached before systemd-sleep acts. Stopping the daemon costs nothing here because `power/wakeup` on the sensor is `disabled` — there is no wake-on-finger to lose. The drop-in is deliberately UNGUARDED on fprintd being installed (contrast the hostname role's avahi guard, which exists only because systemd_service hard-fails on an absent unit): a drop-in dir for an unknown unit is ignored, and writing it unconditionally means the fix is live the first time fprintd is ever activated. The `daemon-reload` handler passes **no `name:`** — naming a unit makes systemd_service probe LoadState and hard-fail when absent (the battery power-profiles-daemon trap), while a bare `daemon_reload: true` touches no unit and stays honest under a fresh `--check`. Handler not converged task: daemon-reload can never report unchanged. Manual escape hatch if it ever wedges mid-session: `sudo systemctl restart fprintd`. **Never diagnose this from `fprintd-list` alone** — it answers from stored prints without claiming the device, so it succeeds while the sensor is wedged; `fprintd-verify` (or `busctl call net.reactivated.Fprint /net/reactivated/Fprint/Manager net.reactivated.Fprint.Manager GetDefaultDevice`) is what actually opens it. Substring: `./install.sh fingerprint` (or `finger`) hits only this role.
- login_keyring: the keyring's key derives from the login password; fingerprint can never unlock it (the sensor yields no key material). The role blanks the keyring password via `library/login_keyring.py` (gnome-keyring's `InternalUnsupportedGuiltRiddenInterface.ChangeWithMasterPassword` — internal but stable, gnome-initial-setup relies on it), which needs the *current* password: a **gated `pause` task** prompts only when the keyring is present-and-encrypted and never under --check (`vars_prompt` was rejected — it fires unconditionally at play start). State detection is file magic, no D-Bus: plaintext `[keyring]` = blank, binary `GnomeKeyring` = encrypted. The password rides a `no_log` module arg over the local connection's stdin (never argv/env). Wrong password fails the task with the Seahorse guidance, nothing modified. Cleanup of the earlier revision's greeter drop-ins (`/etc/dconf/db/gdm.d/…`) is gated on the keyring being confirmed blank — a failed blanking never restores fingerprint login prematurely — followed *immediately* by a `dconf update` gated on `is changed` (the old `removed=1` flag; deliberately not a handler — a deferred flush lost to a later role's failure would leave the compiled gdm db stale with nothing on disk for a re-run to notice). **Blanking does not hold on its own, and what undoes it is a password login — not a package update:** `pam_gnome_keyring` hands the typed login password to gnome-keyring on every *password* login at the greeter, the daemon sees the login keyring's (empty) password disagree with it, and "repairs" the mismatch by re-keying the keyring to the login password — `strings /usr/bin/gnome-keyring-daemon` carries the message verbatim, "fixed login keyring password to match login password" (the code path is `fix_login_keyring_if_unlock_failed()` in `pkcs11/wrap-layer/`, armed by a recorded failed unlock). From then on the keyring is encrypted and `/etc/pam.d/gdm-fingerprint` — which carries NO pam_gnome_keyring line at all — can never unlock it, so every fingerprint login prompts. Traced live 2026-08-16: fingerprint login 01:55 silent, `gdm-password` login 02:00:27 logging "gkr-pam: gnome-keyring-daemon started properly and unlocked keyring", fingerprint logins 02:01:57 + 02:02:37 raising gcr-prompter password prompts, `login.keyring` flipped `[keyring]` -> `GnomeKeyring`; the keyring stack was untouched by that night's dnf transaction (gnome-keyring 50.0 dated from April), so **"a system update broke the keyring" is always this instead** — the reboot is merely when a password gets typed at the greeter. Note it takes a *fresh* greeter login: lock-screen unlocks appear in the journal without the daemon-start line and left the blank keyring alone for days. The role therefore strips the two password-passing lines (`auth`, and `password … use_authtok`) from `/etc/pam.d/gdm-password` AND `gdm-switchable-auth`, anchored per module type so **`session … auto_start` survives** — that line starts gnome-keyring-daemon and needs no password; removing it would trade this bug for a dead Secret Service. `gdm-autologin` (no password is ever typed) and `/etc/pam.d/passwd` (its old-password check just fails against a blank keyring) are deliberately out of scope. Those files are rpm-owned by gdm and flagged **plain `%config`, NOT `%config(noreplace)`** — `rpm -q --queryformat '[%{FILENAMES} %{FILEFLAGS:fflags}\n]' gdm` — so a gdm update whose copy differs reinstalls the vendor file and drops ours to `.rpmsave`; that is why this is a converged task and not a hand edit, staleness bounded to zero runs by the updates role upgrading gdm at the top of the same play (the brave-desktop-shadow argument). The stack list comes from `find` (absent file = empty loop, no hard fail) with `default([])` on the register. Rejected alternatives: greeter password-only (user wants fingerprint), `authselect disable-feature with-fingerprint` (gates only sudo/polkit), an authselect custom profile (these gdm-* stacks are shipped by the gdm rpm, not generated by authselect — `authselect`'s own `with-pam-gnome-keyring` feature is disabled here and touches only `password-auth`/`system-auth`), true fingerprint unlock (impossible; TPM-backed work hasn't shipped in Fedora 44).
- vscode: `roles/dev/vscode/files/vscode.repo` is Microsoft's documented content verbatim — `autorefresh`/`type` are zypper-isms dnf5 silently ignores; keep the file byte-diffable against the docs, and don't install it via `dnf config-manager addrepo` (rejects `autorefresh`, errors on existing files). No `rpm --import`: under Fedora 44's rpm 6, imported keys are named by *full fingerprint*, so the classic `gpg-pubkey-<short-keyid>` guard never matches — the repo file's `gpgkey` + `gpgcheck=1` lets dnf import and verify on install. Microsoft's documented `dnf check-update` step stays dropped (exit 100 when updates exist). The 2025 Microsoft key rotation doesn't affect this repo — yumrepos/vscode still signs with the original `microsoft.asc`.
- claude_code: the native installer has no already-installed check (re-downloads the full ~260 MB binary every unguarded run) — the role guards on `~/.local/bin/claude` (a stable symlink into `~/.local/share/claude/versions/`; the binary self-updates, so presence is terminal) plus a `command -v claude` probe (an npm/dnf install counts; docs warn against stacking copies — and don't probe `~/.claude`, that's config/state shared with the IDE extensions). The installer is downloaded to a temp file then run — never piped into bash (a mid-stream death would execute a truncated script). `get_url` needs `force: true` there because tempfile pre-creates the (empty) dest and get_url's default would skip the download. tempfile has no check-mode support: downstream tasks guard on `.path is defined`. **Node is a hard dependency of the caveman plugin below, installed here** (2026-09-26, discovered when removing the litellm role — which used to install `nodejs22` for Prisma — broke this session's own live caveman hooks mid-task): `.claude-plugin/plugin.json`'s SessionStart/UserPromptSubmit hooks are `node ".../src/hooks/caveman-activate.js"` / `caveman-mode-tracker.js`, stdlib-only (`fs`/`path`/`os`/`child_process` + relative requires between the plugin's own files, no `node_modules`, verified against the installed plugin tree), so a bare Node runtime is enough — exact rpm names `nodejs22`, `nodejs22-libs`, `nodejs22-bin` (no npm/npm-bin: nothing here touches the registry; `nodejs` is a virtual Provides, the vim-enhanced lesson), guarded with the cli_tools difference-filter pattern, before the marketplace/install tasks that need it. **ultracode default** rides here too (last tasks in the role): the settings schema makes `ultracode` SESSION-SCOPED — the in-session toggle never persists it — so the only way to default it is a persistent settings source, `~/.claude/settings.json`. The role MERGES the single `ultracode: true` key (slurp -> `from_json` -> `combine` -> `to_nice_json` copy), never owning the file: Claude Code rewrites settings.json at runtime (model/theme/effort are interactively settable and persisted), so a whole-file copy would fight the app on every interactive change — the same rewrite-at-runtime reason claude_desktop leaves `claude_desktop_config.json` unmanaged. Idempotency gates on the LOGICAL value (`not (claude_settings.ultracode | default(false) | bool)`), NOT file bytes: the canonical `to_nice_json` render won't match Claude Code's own on-disk formatting, so a bytes/checksum comparison would report changed and rewrite every run, flip-flopping against the app forever; value-gating lands the one-time reformat once and skips thereafter (ultracode is a recognized schema key, so the app preserves it across its rewrites). Runs after the caveman install so the slurp reads that write and re-merges its `enabledPlugins`/marketplace keys forward. slurp is `failed_when: false` (file absent on a fresh machine -> parse defaults to `{}`); `$HOME` file, NO become. Needs workflows enabled (default on-plan) + an xhigh-capable `model` (opus qualifies).
- podman: chosen over Docker (Fedora-native, daemonless, rootless — no root-equivalent docker group; the repo's brave-style "one vendor repo fewer" logic too). The package task is deliberately unguarded `state: latest` + `update_cache: true` (aaron wants podman always current; the second network exception). `podman.socket` is enabled at `scope: user` — session bus, NO become, same rule as dconf — and gated `not ansible_check_mode or 'podman' in ansible_facts.packages` (systemd_service fails on missing units even under --check). `/etc/containers/nodocker` uses `state: touch` + `modification_time/access_time: preserve`, the idempotent-touch pattern — a bare touch would report changed every run. Fedora already provisions rootless subuid/subgid ranges at user creation; don't manage them. **Podman Desktop** is an add-on here (meaningless without podman): the Flathub flatpak `io.podman_desktop.PodmanDesktop` via `community.general.flatpak` (become, system scope) — no Fedora rpm exists, the COPRs are one-person electron rebuilds, and Fedora's stock flathub remote already exposes it (verified with `flatpak remote-ls flathub`; the remote is configured with a filter file, but `/usr/share/flatpak/fedora-flathub.filter` has read `# Unfiltered` since F38, so nothing is hidden), so no remote-add and no unfiltering. The module runs `flatpak list --system` before touching anything, so the converged re-run stays offline; guarded on the flatpak rpm like the updates role, whose `flatpak update` keeps it current. It is the first flatpak this repo installs — the hostname role's lock sweep deliberately still ignores `~/.var/app` (see that bullet).
- tailscale: `files/tailscale.repo` is the vendor's file verbatim (both `gpgcheck` and `repo_gpgcheck` are Tailscale's own settings). Login is not automatable — `tailscale up` is a browser flow — so the role probes `tailscale status --json` for `"NeedsLogin"` (BackendState) and prints the one manual step; the probe chain is gated on the binary existing so fresh `--check` stays clean. dnf + systemd tasks carry the standard fresh-`--check` gates.
- reaper: no rpm/repo exists — official tarball's `install-reaper.sh --install /opt --integrate-sys-desktop --usr-local-bin-symlink --quiet` (flags verified against the script), gated on `/opt/REAPER/reaper` (presence is terminal: no self-update on Linux, the updates role can't see it — the same guard-on-the-launcher, never-version-probe rule as claude_code, whose binary self-updates, and install.sh's bw install, which like REAPER never updates). "Latest" resolves at install time from `reaper.fm/latestversion/` (plain text, version is the FIRST line — changelog follows) and the tarball URL is derived (`7.77` -> `files/7.x/reaper777_linux_x86_64.tar.xz`); x86_64 hardcoded (single-machine repo, the intel-media-driver precedent). ALL resource-path work (`~/.config/REAPER`) is user-session — NO become (root's REAPER otherwise). **Theme is Reapertips** (Alejandro Hernandez, CC BY-NC-SA 4.0), restored 2026-08-11 after a three-theme detour — and, like every asset in that section, **VENDORED, not fetched**: the licence permits redistribution, so committing it is legitimate, and it makes a fresh install work with zero network. What ships: the `.ReaperThemeZip` (REAPER loads it ZIPPED — never extract it), 2522 toolbar icons, 303 track icons in 12 category dirs, the Rodilab colour-palette file, the attribution text, and Fira Sans Regular/Bold — no Fedora package exists for it (`dnf5 search fira` is empty), while Roboto is the stock `google-roboto-fonts` rpm (the package also bundles `Roboto-Bold.ttf`; the rpm supersedes it). **The vendored version is discoverable without git archaeology** — the outer filename is deliberately unversioned so `lastthemefn5` survives updates, but the folder INSIDE the zip carries it: `python3 -m zipfile -l 'roles/audio/reaper/files/Reapertips Theme.ReaperThemeZip'` -> `ReaperTips_v1.93/` (currently the v1.93b package; the copy before it was `Reapertips_v1.90_Dark/`). **It is the DARK variant, verified by colour rather than by the guide's word:** the package ships the main theme plus five variations, and the main one is the dark build — `col_arrangebg` #424242 / `genlist_bg` #323232 / `col_main_text` #a0a0a0, where Light is #bdbdbd/#d1d1d1/#242424, Hybrid lightens the arrange to #a3a3a3, and "Dark with Light Menus" (`_LM`) keeps #424242 but flips lists to #d2d2d2. The variations are deliberately NOT vendored (~3.5 MB each, single-machine repo). **The two icon prefixes are two different PRODUCTS, and confusing them makes a theme update delete the expensive half:** `RTH_` (257 = 86/86/85 across the 1x, 150 and 200 scale dirs) is the THEME package's own icon set, shipped in its `04_Toolbar Icons` and refreshed by a theme update; `RT_` (2265 = 755 icons × 3 scales) plus all 303 track icons come from the **separate "Essential Icons for REAPER" pack**, a different paid download that is NOT in the theme zip — a theme update must leave those alone. (`150`/`200` are SCALE dirs; both prefixes appear at all three scales, so "the RTH_ set is the HiDPI half" — as an earlier revision of this file implied — is wrong.) Zero filename overlap between the two products was verified; the 3 icons unique to the Essential pack's `Update/` dir are included, its other 123 being byte-identical to the main pack. Icon dirs merge ADDITIVELY (rsync, no `--delete`): REAPER treats `Data/` as a user-extended pool, not a repo-owned tree — the opposite of gnome_extension's mirror. **Updating the theme** (2026-08-11, v1.9 -> v1.93b) came down to two files: the `.ReaperThemeZip` and `!license.txt` (its first line is the version, which read `v1.94` in the v1.93b package — the author's own inconsistency, not a mistake to "fix"). The pack's `libSwell-user.colortheme`, colour palette, both Fira Sans ttfs and all 257 `RTH_` icons were **byte-identical** to what was already vendored — diff before replacing, and never `rm -rf` the icon tree to "start clean". **The dark `libSwell-user.colortheme` override rides with this theme and is THEME-COUPLED, not universal:** the tarball build has no GTK theming, so that file IS "dark mode" on Linux for dialogs and menus — the thing REAPER exposes as a real setting only on Windows — and Reapertips ships it, in the package's `03_Extras/Linux Colortheme` with a `!howtouse.txt` that says only "drop it in the resource path root" (installed verbatim). It is what COMPLETES the dark variant: the theme paints its own lists at #323232 with #a0a0a0 text, so stock Swell dialogs would be the only light surfaces left in the app. **That file is also the only lever REAPER has for the SIZE of its Linux dialogs, and it is NOT colours-only:** its first nine lines are metrics (`default_font_size`, `menubar_height`, `menubar_font_size`, `menubar_spacing_width`, `menubar_margin_width`, `scrollbar_width`, `scrollbar_min_thumb_height`, `combo_height`, plus `default_font_face`), first-class documented keys that REAPER ships identically at the top of its own reference template `/opt/REAPER/libSwell.colortheme` — the place to check what else is settable. Reapertips leaves all nine at stock, and stock is cramped on this panel, which is what makes the Media Explorer / action list / FX browser / Preferences fiddly. So the role does NOT plain-copy that file: `templates/libSwell-user.colortheme.j2` renders the vendored file line-for-line with `reaper_swell_metrics` (task vars) substituted in — font 13->**14**, menubar 17->20 with 12->14 text, scrollbar 14->17 with the 4px minimum thumb ->20, combo 20->24. Structuring it that way is the point: **the vendored asset stays byte-identical to the package** so the next theme update is still diffable against it, and the overrides live somewhere visible instead of being edited into a blob where the next update would silently revert them. `default_font_size` is the one value with a trade-off — SWELL dialogs are fixed pixel geometry, so a large jump CLIPS labels in dense dialogs (check Preferences); the scrollbar/combo values carry no such risk, and being independent of the font they let text size and click-target size be traded off separately. **Granularity, asked and answered (2026-08-11): 15 overshot, and 14 is the ONLY step in between** — the key is integer pixels, there is no fractional size. The only sub-step lever is `default_font_face`, and it barely moves: measured x-height/em is 0.5283 Liberation Sans (the vendor's face), 0.5283 Roboto, 0.5270 Fira Sans, 0.5360 Noto Sans (~14.2px apparent at 14), 0.4820 Cantarell (a real step DOWN if 14 ever reads too big). DejaVu Sans is the one face with a meaningfully larger x-height and is NOT installed — don't add a font package for ~3%. For proportionally bigger EVERYTHING (arrange/TCP/MCP/transport, not just dialogs) the knob is REAPER's global UI scale, not this file. Applied on theme (re)load, not only at startup — verified live, a REAPER running for two hours picked the metrics up when the theme was reloaded (so the earlier "requires a restart" note was stricter than reality; a restart works too). **Template whitespace is load-bearing there:** ansible renders with `trim_blocks=True`, which eats the newline immediately after `{% endif %}`/`{% endfor %}`, so each emitted line's newline must follow an EXPRESSION tag — written the obvious way the loop collapses all 93 lines into one and SWELL parses only the first key (caught by rendering it; same family as the reaper-mouse.ini escape trap). **There is no touchscreen on this machine** (verified — `/proc/bus/input/devices` lists only the keyboard, TrackPoint and Synaptics touchpad; the T14 Gen 7 21WNCTO1WW is a non-touch config), and REAPER's binary carries no touch/pen preference strings on Linux while SWELL discards gestures outright, so "touch-friendly REAPER" here means hit-target size — these metrics plus the theme's own 90 `define_parameter` sliders (`trans_height`, `tcp_list_row_h`, `mcp_list_row_h`, `mcp_wide_w`…) and REAPER's global `uiscale`, not any touch mode. It was deleted for exactly one revision, while the theme was Default-7.0-derived, and that is the lesson to keep — LCS_Flat-7 specifies near-black dialog text (`col_main_text #1e2222`) on light list/edit grounds (`genlist_bg #dcdede`) because that is what stock REAPER dialogs are, so forcing Swell's backgrounds dark put black text on a dark ground and made button labels **invisible**. Rule: the Swell override travels with the theme — dark theme yes, stock-derived light-dialog theme no. **Retired themes, kept on the record; the machine-side sweep is a permanent `state: absent` for LCS_Flat-7's zip + notice** (the claude_desktop precedent — rLive and paRt were fetched, never vendored, and are already gone): **LCS_Flat-7** (lucas_LCS) is REAPER's stock Default 7.0 with every button flattened — LGPL by copyleft propagation, so it was vendorable too (stash.reaper.fm serves files only behind a browser-side SHA-1 proof-of-work "verify you are human" page, which no `get_url` can ever pass, so fetching was never an option) — flat and ergonomic with no adjuster wiring, but it reads as generic stock REAPER, which is what sent this back to Reapertips. Its lasting lesson: **theme parameters cannot be set declaratively** — `ThemeLayout_SetParameter(.., persist=true)` from a startup script neither took effect nor wrote anything to reaper.ini (verified), so the only reliable lever is editing a vendored theme's `define_parameter` default and repacking. **paRt** is flatter still and GitHub-hosted, but its controls are tiny by default and every fix lives behind a Theme Adjuster and a bank system whose scripts must first be hand-registered as REAPER actions (`SCR` lines in reaper-kb.ini) before its transport bank buttons do anything at all. **The Ableton-theme hunt is a CLOSED DEAD END; do not reopen it.** No REAPER-7-compatible Ableton Live theme exists. All four candidates are abandoned and unlicensed, and the two serious ones were empirically tested here and BOTH fail: **rLive** (v5) loads its colours and images but REAPER 7.78 never applies its WALTER layout, so you get stock TCP/MCP wearing rLive's palette — a hybrid that reads as neither; **ReaperA** (v6) does apply its layout but ships ONE toolbar image (stock has 129, paRt 3817), so its toolbar degrades to truncated text labels. Renaming rLive's zip to match its internal theme name, the Theme Adjuster, the ModPack and LBX Stripper were all tried or ruled out — none can fix a layout engine that never runs; rLive's screenshots are REAPER-6-era. **MyDaw** (sineforge) is the only thing that really delivers Ableton-in-REAPER and it is Windows/macOS only, no `.so`. **Diagnostic trap that cost hours:** two themes can be told apart ONLY by their WALTER layout, not by colour sampling — a "good" render was mistaken for rLive for a long time when it was actually still Reapertips, because `lastthemefn5` pointed at rLive while REAPER had silently fallen back. Always verify a theme swap by cropping the TCP and diffing against a known baseline, never by eyeballing the palette. **Launching REAPER from a non-GUI shell breaks it silently:** SWELL calls the X11-specific `gdk_x11_*` API, so with `GDK_BACKEND=wayland` in the environment GDK opens a Wayland display, every `gdk_*` call gets NULL, and REAPER starts **windowless** while still holding its single-instance lock — which then swallows the user's own launcher clicks. gnome-shell sets no `GDK_BACKEND`, so GUI launches are fine; only tooling shells are affected (`env -u GDK_BACKEND` to test). **Nuking ~/.config/REAPER destroys `reaper-license.rk`** (the purchased licence) and the ALSA device config — back the directory up before any such experiment. The Ableton work that SURVIVES is the navigation half, which is theme-independent. **Reapertips' own "Perfect Setup" guide is VENDORED at `docs/reapertips-perfect-setup.pdf`** (80pp, ~19 MB, same author as the theme; `docs/` exists for exactly this — provenance that is not an installed asset, so it does NOT go in `roles/audio/reaper/files/`, where everything else gets copied to the machine). It is the source for most of `templates/reaper.ini.j2`, and the page numbers in the role's manual-steps debug refer to it. **The verification rule is what decides what gets encoded:** mespotine's config-var reference (`Docs/Reaper-ConfigVariables-Documentation.txt` in github.com/mespotine/ultraschall-and-reaper-docs — download and grep it, don't fetch the HTML mirror, which 404s) is **REAPER 5.95-era**, so every REAPER-7-only tip in the guide — the item selection indicator (p21), folder-collapse modes (p32), visual track spacer size (p35), render block size (p60), auto record-arm on select (p51) — has NO documented variable and goes in the nudge with its page number rather than being reverse-engineered. Same for `projtakelane` (p49): it IS in the reference but its entry lacks the boilerplate "Stored in reaper.ini under the same name in the section REAPER", and that sentence's presence/absence is the actual test for whether a key belongs in the template. Encoded keys and their arithmetic: `splitautoxfade=40` (p39; `&8` is INVERTED — 1 turns the auto-fade checkbox OFF — and `&32` is REAPER-7-era and undocumented, so 40 is taken WHOLE from a live 7.78 with the guide applied rather than reconstructed), `hwfadex=1` (p40; `&1` fade-out-on-stop kept, `&2` fade-in-on-start cleared), `itemicons=823807` (p41; 511 for the nine button bits + 4096 for both Item Properties boxes off + 32768 for REAPER's own fade-handle default + 262144 + 524288), `recaddatloop=23044` (p50; `&4` discard incomplete, and the threshold percentage is a plain 7-bit number one byte up, so 90*256+4), `midieditor=33` (p65/p68; `&1` one-editor-per-project, `&32` close-on-delete unchecked), `loopnewitems=0` (p42), `projgridmin=18` (p26). **Bitfields are spelled out bit by bit in the template because writing one key sets ALL of its bits** — a value pasted without the arithmetic silently changes preferences the guide never mentioned, which is why `itemicons` declares REAPER's fade-handle default explicitly instead of pretending it isn't being set. **The seed alone reaches nobody who has already run REAPER**, so the keys worth having are ALSO listed in `reaper_ini_keys` (a `vars:` on the probe task in tasks/main.yml) and back-filled with `lineinfile` + `insertafter: ^\[REAPER\]` — probed ONE KEY AT A TIME (a joint probe skips the whole set the moment any one member exists) and added only where absent, so REAPER's rewrite-at-exit and any in-app change always win. Template and `reaper_ini_keys` must be kept in sync; the template header says so. **Audio device keys (2026-09-12):** `linux_audio_mode` is deliberately NOT seeded — its default 0 IS JACK (values sandbox-verified, see the pipewire bullet), so an ini without the key is already on PipeWire's JACK; what IS seeded and back-filled is `linux_audio_nch_in=16` / `linux_audio_nch_out=8`, because REAPER's stock 2/2 opens two JACK ports, auto-connected to the DEFAULT input device (the laptop mic) — an 8-in interface behind it is invisible until the count is raised. Back-fill is absent-only, so aaron's own ini (2/2, written by REAPER) keeps its numbers; the checklist item 21 and the pipewire nudge say so. **Two deliberate conflicts between the guide and Ableton parity, both resolved on the record:** `loopnewitems` was REVERSED from 1 to 0 (the guide's p42 extend-don't-loop beat the "a Live clip loops when you drag its edge" reasoning — and aaron's own machine had already landed on 0, which settled it), while `zoommode` stays 0 and does NOT take the guide's p25 mouse-anchored horizontal zoom (Live anchors on the selection; `vzoommode=3` already matches the guide because Live's Option+scroll IS pointer-anchored). **What stays manual and why**, beyond the undocumented keys: mouse-modifier behaviour IDs (p37 multi-fade, p43 limit-item-edges, p62 one-click note insert) are per-context indices REAPER only reveals through its own "Export ReaScript to set these modifiers for this context" button; and the p47 E/Q record shortcuts are NOT seeded even though both action IDs were verified (`1013` Transport: Record, `40668` Transport: Stop (DELETE all recorded media) — note 40668, not 40667) because the plain-letter KEY encoding in mespotine's `Reaper-KEY-Codes_for_reaper-kb_ini.ini` is self-contradictory (`0_101=E` follows lowercase ASCII but `0_81=Q` follows uppercase, and `0_113` is empty), which is the same "not documented well enough to seed blind" bar the Z/X zoom bindings already fail. The nudge fires on any run that actually changed reaper.ini — seed OR back-fill (`reaper_ini_backfill is changed`) — not seed-only, or a configured machine would never see the checklist. **A lot of the guide is already covered elsewhere in the role and must not be re-added:** SWS (p76) and ReaPack (p77) are installed tasks, hand-scroll on middle-drag (p23) is the `MM_CTX_ARRANGE_MMOUSE` seed, wheel scroll/zoom (p24) is the reaper-kb.ini wheel block, and the theme (p30) is the whole first half of this bullet. 
- pipewire: two user-session drop-ins, NO become — `~/.config/wireplumber/wireplumber.conf.d/50-pro-audio-interfaces.conf` (templated from `pipewire_pro_audio_devices` in site.yml) and `~/.config/pipewire/jack.conf.d/50-daw-latency.conf` (from `pipewire_jack_latency`). **Why the profile rule exists:** WirePlumber's ACP ranking follows the USB descriptor's channel map, and the Quad Cortex mini (USB 152a:892f, Thesycon UAC2, 8 in/8 out, 48 kHz only, S32_LE) declares FL FR FC LFE RL RR FLC FRC — so stock Fedora files it as "Analog Surround 7.1 Output + Input" (EnumProfile index 1, priority 1212), opened through alsa-lib's `surround71:1` plug, which REMAPS channels (ALSA ch 2 → capture_RL, ch 4 → capture_FC) and names them after speakers. Pro Audio opens `hw:1,0` raw as AUX0..7, node "<card> Pro". `find-best-profile.lua` skips pro-audio on purpose, so it must be asked for: `device.profile.priority.rules` (WirePlumber 0.5, `scripts/device/find-preferred-profile.lua`) with `priorities = ["pro-audio"]`, matched on `device.vendor.id`/`device.product.id` (strings, "0x152a") — not `device.name`, which carries the product string and a `-00` unit suffix. **Precedence (read the scripts, not the docs):** find-stored-profile runs BEFORE find-preferred-profile, so a hand-picked profile (Settings > Sound, `wpctl set-profile`, both set `save`) recorded in `~/.local/state/wireplumber/default-profile` wins over the rule forever — the bookmark rule; the role never touches the state file. **False-verification trap:** `wpctl set-profile` WRITES that state, so a rule tested right after a manual switch proves nothing — delete the entry, `systemctl --user restart wireplumber`, then read `pw-dump <device id> | jq '.[0].info.params.Profile[0]'` — the rule yields `name: pro-audio, save: false`, a stored choice `save: true` (the scripts' "Found preferred profile" line is log:info and never reaches the journal at the default log level, so don't grep for it). Verified that way 2026-09-12: state file absent, `save: false`. WirePlumber reads conf.d only at startup (no reload) — the handler restarts it (user scope, guarded plainly on the package, the hostname/avahi form: this role installs nothing that provides the unit); jack.conf.d is read per client, no restart. **Pro Audio does NOT lower the default-sink priority** — the Pro sink carries priority.session 1600 (vs 1109 in 7.1, 1000 for the laptop's HDA), so plugging the mini in still steals the default output; the default input stays on whatever `default.configured.audio.source` says (aaron's is the HDA mic). Deliberately not managed. **pipewire-jack facts, all verified 2026-09-12:** port names are `<node.description>:<port.name>`; physical ports list the DEFAULT sink/source first, then the rest — so REAPER's auto-connected input numbering depends on the Settings > Sound input choice; a JACK client's latency request is what drops the graph quantum (`node.latency = 128/48000`), and `node.lock-quantum = true` is there because REAPER registers process/sample-rate/xrun callbacks but NO buffer-size callback (checked in its strings). 128 measured xrun-free with pw-record + pw-cat at once (~13 µs busy per 2.67 ms cycle) — note `pw-cat --latency` takes `128` or `2.7ms`, NOT `128/48000` ("bad unit"), and `pw-top -b -n 1`'s first frame is a placeholder with QUANT 0 (use `-n 3` and read the last table). **REAPER facts** (sandboxed launch: `HOME=<scratch> XDG_CONFIG_HOME=<scratch>/.config env -u GDK_BACKEND reaper -newinst -nosplash probe.lua` with the licence file copied in — a ReaScript on argv runs at startup, `reaper.get_config_var_string`/`GetAudioDeviceInfo("MODE")`/`GetInputChannelName` read the state, `Main_OnCommand(40004)` quits; the real reaper.ini was sha256-checked untouched): `linux_audio_mode` 0 = JACK (the default — an ini WITHOUT the key is in JACK mode), 1 = ALSA, 2 = Dummy Audio, 3 = PulseAudio; `linux_audio_nch_in/out` default 2/2 and are honoured in JACK mode (16 → 16 ports, the extras "not connected"); inputs are labelled by the connected port, `Quad Cortex mini Pro:capture_AUX0 (Input 3)`; `jack_options=0` STILL auto-connects, so its bit 1 is not the auto-connect box — don't seed it; JACK MIDI ports include `Midi-Bridge:Quad Cortex mini: MIDI 1 (capture)`. `pgrep -x reaper` is the guard for that launch — `pgrep reaper` matches the kernel's `[oom_reaper]`, and `pgrep -f /opt/REAPER/reaper` matches the shell running the script itself. The ALSA-direct alternative (`alsa_indev=hw:mini`, what aaron's ini still says for the retired Fender `hw:Q16`) was rejected: it needs PipeWire to release the card, which the default-sink hijack guarantees it won't. Substring: `./install.sh pipe` / `wire` hit only this role.
- qpwgraph: PipeWire patchbay GUI, one guarded dnf task (stock Fedora rpm). A SEPARATE role, not a reaper add-on — system-wide audio routing is meaningful without REAPER, so the dependency-direction merge test fails (the github_cli call, not the caveman one). Chosen over Helvum (GTK-native) because qpwgraph saves persistent patchbay files that auto-reconnect — the feature a recurring REAPER routing needs. Qt-on-GTK look is cosmetic only; don't "fix" it with theming packages.
- mise: replaces pyenv/nvm/jenv (installs and pins runtimes; reads `.nvmrc`/`.python-version`). `files/mise.repo` is the vendor's file verbatim — note the baseurl is `mise.en.dev` while docs live at `mise.jdx.dev` (hosting move, both endpoints verified live; don't "fix" the URL). The shell hook lives in `roles/dev/zsh/files/zshrc`, guarded on `command -v mise` — it must stay in the zsh role's asset: a lineinfile from the mise role would diverge `~/.zshrc` from the asset and the zsh role's cmp-copy would clobber it on the next run. Runtimes are deliberately not provisioned (per-project `mise use` decisions).
- claude_code / caveman plugin (same role — pure add-on, merged): the plugin installs via `claude plugin marketplace add` + `claude plugin install` as the user — state in `~/.claude/plugins/` (known_marketplaces.json, marketplaces/, cache/, installed_plugins.json). The marketplace source is the **https URL**, not the `owner/repo` shorthand: the shorthand clones over `git@github.com:`, which fails on a fresh machine whose key isn't enrolled with GitHub yet. The `claude plugin list` probe has `failed_when: false` and its rc gates the plugin tasks — under fresh `--check` the CLI install above was only simulated, so the binary doesn't exist and a hard probe would crash the dry run. Guards probe *presence*, not enabled-state — installing enables the plugin, and re-enabling every run would fight a deliberate `claude plugin disable`. `claude` runs with `~/.local/bin` prepended to PATH (the play's PATH may lack it before the zshrc takes effect at next login).
- claude_desktop: separate role from claude_code, NOT an add-on — the GUI chat app is meaningful without the CLI (the reason it's here is local stdio MCP, which a PWA/Electron-wrapper of claude.ai can't do), and neither depends on the other, so the merge test fails (contrast the caveman plugin, which IS meaningless without the CLI). Anthropic ships no Fedora build — the official Linux beta is Debian/Ubuntu-only ("Fedora and RHEL: only Debian-based distributions are supported today") — so this installs `claude-desktop-unofficial` from aaddrick/claude-desktop-debian's signed DNF repo, which since v3.0.0 repackages Anthropic's *official* Linux `.deb` (thin repack, low breakage risk vs the pre-v3 Windows-app-extraction era). Pure vendor-repo role (vscode/tailscale pattern): repo file + dnf5, package-facts guard + fresh-`--check` gate. Updates ride the `updates` role's `dnf upgrade` — the app doesn't self-update on Linux (Anthropic's docs; dnf is the intended channel), so no self-updater to fight. Two decisions encoded: (1) `files/claude-desktop-unofficial.repo` is the vendor file verbatim EXCEPT an added `skip_if_unavailable=1` — deliberate deviation from the file-verbatim convention because it's a single-maintainer self-hosted domain and the updates role runs `dnf upgrade` on `*`, so a dead host would hard-fail every system-update run (the ghostty COPR file is the precedent for a repo file carrying this flag). (2) The install guards on the exact rpm name `claude-desktop-unofficial`, never the virtual `claude-desktop` it Provides — virtual-provides matching is forbidden by convention, and a future official Anthropic rpm named `claude-desktop` would false-match and silently skip. gpgcheck=1 + repo_gpgcheck=1 with the gpgkey URL in the file, so dnf imports/verifies the maintainer's key on install — no rpm --import (the vscode rpm-6-fingerprint lesson). MCP config lives at `~/.config/Claude/claude_desktop_config.json` — the role deliberately does NOT manage it (per-user server choices, and the app rewrites the file on exit). Substring-matcher note: `./install.sh claude` now matches BOTH claude_code and claude_desktop (both dir names contain "claude") — running it reaches both roles; use `claude-desktop` / `claude-code` to target one. Trust: unofficial-squared (unofficial packaging of an app Anthropic doesn't ship for Fedora), maintainer's GPG key not Anthropic's — accepted deliberately; the rpm's `Provides: claude-desktop` eases a future migration to an official Fedora rpm. **Window↔launcher binding — the launcher override was REMOVED (2026-08-07); do not reintroduce it.** An earlier revision shadowed the vendor entry with `files/claude-desktop.desktop` at the same basename in `~/.local/share/applications/`, patching `StartupWMClass` to `claude-desktop` (the vendor then shipped `StartupWMClass=Claude` while Electron advertised `claude-desktop` — aaddrick #588). Upstream has since fixed it: `launcher-common.sh` pins `readonly WM_CLASS='com.anthropic.Claude'` (substituted at package time from the staged app.asar's `desktopName`), passes `--class="$WM_CLASS"`, and the vendor `.desktop` ships `StartupWMClass=com.anthropic.Claude` — verified live, `xprop -id <win> WM_CLASS` -> `"com.anthropic.claude", "com.anthropic.Claude"`. So the shadow became the only mismatch on the machine, and because `~/.local/share/applications` OUTRANKS `/usr/share` (entries replace rather than merge across XDG dirs) the stale copy silently beat the upstream fix: window bound to no app, blank icon in alt+tab, while app search still looked right (it reads `Name=`/`Icon=` off disk — no live window involved, which is what makes this fail quietly). The role now carries a `state: absent` task for the override path (machines that ran the old revision still have the file) plus the on-change `update-desktop-database`. **General rule for this repo: never shadow a vendor `.desktop` to patch `StartupWMClass`** — a copied value is a snapshot of something the vendor owns and changes (`Claude` -> `claude-desktop` -> `com.anthropic.Claude` in three releases) parked in the dir that always wins. Diagnose instead: compare the live `xprop WM_CLASS` (X11) / app_id (Wayland) against the entry's `StartupWMClass`, and fix it upstream. The rule is about SNAPSHOTS, not about shadows as such: brave shadows `brave-browser.desktop` to rename the app, and that is sanctioned because the shadow is **regenerated from the vendor file on every run**, so no vendor-owned value is ever frozen — if a shadow is ever unavoidable again, generate it, never vendor it, and never hand-write a value the vendor owns. Audited 2026-08-07 — every other GUI app this repo installs binds correctly: `StartupWMClass` matches live class for claude-desktop-unofficial, cockos-reaper (`REAPER`), brave-browser, code (`Code`), com.mitchellh.ghostty; steam and btrfs-assistant ship no `StartupWMClass` but their class matches the basename fallback, and qpwgraph calls Qt's `setDesktopFileName` (`org.rncbc.qpwgraph`, matching its entry). btop/podman-tui are `Terminal=true` — their window is Ghostty's, by design.
- herdr: terminal multiplexer with built-in AI-agent-state awareness (github.com/herdrdev/herdr, herdr.dev), the sole member of the `ai` group added alongside claude_code/claude_desktop when ollama/litellm/goose/opencode were fully reverted (2026-09-20 — see git history for that change, not this file). No Fedora rpm or COPR. Upstream ships prebuilt Linux binaries with NO archive and no release-asset checksum file — the trust anchor is `https://herdr.dev/latest.json` (TLS from the vendor's own domain; the same manifest herdr's own `install.sh` and `herdr update` read), an object keyed `version`/`assets`/`sha256` per `<os>-<arch>` target (`linux-x86_64` here) — `roles/common/tasks/herdr_install.yml` (parameter: `herdr_manifest`, the parsed manifest) does a single checksum-verified `get_url` straight to `~/.local/bin/herdr`, no tempfile/unarchive needed (one binary, no archive) and no become in that include (a per-user CLI/TUI, not a system service). get_url's own checksum-skip behavior (it does not re-download when the destination already matches the given checksum) is what makes the same include idempotent for both a first install and a converged re-run, so the role only needs the usual presence guard (`~/.local/bin/herdr` stat) plus the reaper/claude_code "presence is terminal" shape — a lookup against herdr's own manifest instead of a GitHub redirect, `uri` skipped under `--check` (no `check_mode: false` in the role's own probe, verified `ansible-doc`: `uri` check-mode support is `none`) so the include guards on the registered `.content` rather than crashing a fresh dry run. herdr has its own `herdr update` subcommand (verified only as far as `herdr --version` and `herdr update --help` — the latter shows a plain `--handoff` flag and no prompt text, so it reads as non-interactive, but the actual download-and-swap was NOT run live here), which the user can run inside a herdr session for a sooner update than this role provides — so "bring the system current" still needs the `updates` role's own version-compare-and-reinstall (`herdr --version` prints a clean `herdr X.Y.Z`, parsed with `regex_search('[0-9][0-9.]*')`; both the version-read and the manifest-fetch tasks carry `check_mode: false` there — unlike the role's own probe — so a `--check` run can honestly report a pending upgrade; the upgrade task itself is left ungated on check mode, since `get_url`'s partial check-mode support already predicts the change without downloading — see the updates bullet). herdr IS the terminal when it runs (a tmux-shaped TUI/server) — the role never launches it, and there is nothing to configure: it only watches panes for known agent CLIs by process name/output pattern, never talks to a model API itself, so `~/.config/herdr` is entirely the user's. **Desktop launcher and icon** ride here too: `templates/herdr.desktop.j2` renders `Exec=ghostty --confirm-close-surface=false --fullscreen=true -e <HOME>/.local/bin/herdr` (Jinja `{{ lookup('env', 'HOME') }}`, + a matching `TryExec=`) — an ABSOLUTE path, not a bare `herdr` Exec, because gnome-shell resolves `Exec=` against its OWN environment, verified live as `PATH=/usr/local/bin:/usr/bin` (`tr '\0' '\n' </proc/$(pgrep -xo gnome-shell)/environ`) — no `~/.local/bin` — so a bare Exec would open Ghostty, fail to find herdr, and the window would just close with nothing visibly wrong. `-e` rather than `Terminal=true` + Exec: `Terminal=true` would route through GNOME/GLib's own default-terminal resolution (`org.gnome.desktop.default-applications.terminal`), a separate and less predictable mechanism than naming the one terminal this repo actually installs; Ghostty's documented `-e` behavior disables shell-expansion and exits Ghostty when the given command exits, so no shell lingers behind a closed herdr session. **The two Ghostty flags are scoped to the launcher's window only**, and must precede `-e` (everything after it becomes herdr's argv): `-e` forces `gtk-single-instance=false` ("a new instance is launched and the CLI args are respected", Ghostty 1.3.1's own `+show-config --docs`), so they never reach the user's other Ghostty windows. `--confirm-close-surface=false` drops Ghostty's "the running process will be killed" close dialog, a false alarm here: closing the window kills only the herdr CLIENT — `herdr server` runs in its own session with no controlling tty (verified: separate SID from the Ghostty pty, 12h+ uptime across window closes), so agent panes keep running and `herdr` re-attaches. Ghostty 1.3.1 has no per-command confirm rule (`confirm-close-surface` is its only confirm key), so a `herdr` typed into an ordinary Ghostty tab still warns; the global switch would go in the ghostty role's `files/config` and is deliberately NOT set — the dialog is a real guard for every other process. `--fullscreen=true` is native fullscreen at launch (`--maximize=true` is the swap to keep the top bar); the only exit is Ghostty's `ctrl+enter` (`toggle_fullscreen`), since GNOME's own `toggle-fullscreen` is unbound (window_snapping resets it to its empty default) and herdr's defaults bind no ctrl+enter. The launcher `template` and the icon `copy` are both unconditional (no `herdr_bin` guard): both are naturally idempotent, so this stays converged whichever run installed the binary and self-heals a machine that ran an older revision of this role without the launcher. The official herdr logo (a ram's head whose horn traces a `>_` prompt) is vendored from `https://herdr.dev/assets/logo.png` (512×512, fetched once at authoring time — no get_url here, no upstream checksum for a marketing asset the way herdr_install verifies the binary, and fetching it every run would violate the offline-converged-re-run rule for zero gain), pre-scaled at authoring time with ImageMagick (Lanczos) into the same hicolor sizes brave's custom lion uses (16 24 32 48 64 128 256) plus 512 itself — the reaper/brave pre-scaled-asset precedent, no ImageMagick on the target — with the 512×512 master kept beside the tree as `icons/herdr-master.png`, never installed (the task copies only `icons/hicolor/`). Both `update-desktop-database` and `gtk-update-icon-cache -f -t` are inline `is changed`-gated commands with `# noqa: no-handler`, not handlers: `roles/desktop/brave` already owns handlers named exactly "Refresh the desktop database" and "Refresh the user icon cache", and since handler names resolve play-wide (see Conventions), a second handler here with either name would collide with brave's rather than staying scoped to this role — the claude_desktop precedent. Substring: `./install.sh herd` hits only this role (`her` alone would too, since no other role name contains it).
