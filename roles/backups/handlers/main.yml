---
# Unit files changed → make the running user systemd re-read them. Fires
# only on change (the unit copies notify it), the battery-handler pattern;
# no unit name, so the not-found LoadState trap doesn't apply.
- name: systemd user daemon-reload
  ansible.builtin.systemd_service:
    scope: user
    daemon_reload: true
